# 🥗 Healthy Lifestyle Planner

**Healthy Lifestyle Planner** is a desktop GUI application built with **Python (Tkinter)**. It allows users to create personalized weekly plans by selecting sports, meal plans, and daily activities either manually or randomly.

## 🌟 Features

* **Excel Integration:** Import data directly from Excel (`.xlsx`) files.
* **Randomize:** Automatically generate a random weekly plan with one click.
* **Manual Selection:** Add specific items to your plan manually.
* **Export Options:** Save your weekly plan as `.txt`, `.json`, or `.xlsx`.
* **User-Friendly Interface:** Clean layout using `tkinter` and `ttk`.

## 📂 Project Structure

* `main.py`: The main application script.
* `data.xlsx`: Sample dataset containing sports, food plans, and activities.
* `sample_outputs/`: Examples of exported plans generated by the app.

## 🛠️ Requirements

To run this application, you need Python installed and the `openpyxl` library for Excel operations.

```bash
pip install openpyxl

```

## 🚀 How to Run

1. Clone the repository:
```bash
git clone [https://github.com/zeynepgurbuzz/healthy-lifestyle-planner.git](https://github.com/zeynepgurbuzz/healthy-lifestyle-planner.git)

```


2. Navigate to the project folder.
3. Run the application:
```bash
python main.py

```


4. Click "Import Excel File" and select `data.xlsx` to load the items.

---

Author: Zeynep Gürbüz

